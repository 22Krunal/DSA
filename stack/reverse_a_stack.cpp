#include<bits/stdc++.h>

using namespace std;
void reverse(stack <int> *&s){
    s
}
int main(){
    int a[]={1,2,3,4,5};
    stack<int> first;
    for(auto element:a)
    first.push(element);
    return 0;
}
